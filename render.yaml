services:
  - type: web
    name: tay-analytics-api
    runtime: python
    buildCommand: pip install -e .
    startCommand: uvicorn tay.api.app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ALLOWED_ORIGINS
        value: https://tay-analytics.vercel.app  # update after Vercel deploy
    healthCheckPath: /health
